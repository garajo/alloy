The files in the assets directory are here for demo purposes only
In actual application, the data generated by the generateApgDemoData class would
be replaced by code accessing the "real" properties.

The cityType and propertyType enumerations demonstrate useful concepts,
and the use of similar enumerations is encouraged.  (It avoids typing errors.)