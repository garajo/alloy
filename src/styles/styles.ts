import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'alloy-styles,[alloy-styles]',
    template: '',
    styleUrls: ['./styles.scss'],
    encapsulation: ViewEncapsulation.None
})
export class AlloyStylesComponent {}
